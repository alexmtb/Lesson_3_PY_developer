1. Из всех методов списка (list) выбрать 5 тех, которые по вашему мнению используются чаще всего
2. Написать их через запятую с параметрами
3. Повторить процедуру для словарей (dict), множеств (set), строк (str)
Задание творческое правильного ответа нет, предлагаю логически подумать какие методы наиболее популярные

1. Методы списка (list), используемые чаще всего

len(list()), list.append(x), list.count(x), list.insert(i, x), list.remove(x)

2. Методы словарей (dict), используемые чаще всего

dict.fromkeys(seq[, value]), dict.items(), dict.keys(), dict.values(), dict.update([dict_2])

3. Методы множеств (set), используемые чаще всего

set.discard(x), set.update(x), set_1 | set_2 (set_1.union(set_2)), set_1 & set_2,  set_1 ^ set_2

4. Методы строк (str), используемые чаще всего

len(str), str_1.find(str_2, [start],[end]), str.split('символ'), s.replace(шаблон, замена[, maxcount]),
str.join('список')
